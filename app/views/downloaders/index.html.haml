%p#notice= notice


%div{:class => "row"}
  %div{:class => "col"}
    %h1 Downloaders

    %br/
    = link_to 'New Downloader', new_downloader_path, :class => "button primary"


    %table{:class => "striped"}
      %thead
        %tr
          %th Ftp path
          %th Rename
          %th Rename prefix
          %th Instance
          %th Priority
          %th Backup Enabled
          %th Backup Path
          %th Last Download
          %th Processing
          %th{:colspan => "3"}
      %tbody
        - @downloaders.each do |downloader|
          %tr
            %td= downloader.ftp_path
            %td= downloader.rename
            %td= downloader.rename_prefix
            %td= downloader.setting.instance_name
            %td= downloader.interval
            %td= downloader.backup
            %td= downloader.backup_path
            %td= downloader.start_time
            %td= downloader.processing_status
            %td= link_to 'Show', downloader
            %td= link_to 'Edit', edit_downloader_path(downloader)
            %td= button_to "Destroy this downloader", downloader, form: {data: {turbo_confirm: "are you sure?" } }, method: :delete