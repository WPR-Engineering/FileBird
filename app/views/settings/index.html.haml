%p#notice= notice
%h1 Settings
%table
  %thead
    %tr
      %th Instance name
      %th Ftp server
      %th Ftp port
      %th Temp DL Path
      %th WO Mount Path
      %th{:colspan => "3"}
  %tbody
    - @settings.each do |setting|
      %tr
        %td= setting.instance_name
        %td= setting.ftp_server
        %td= setting.ftp_port
        %td= setting.temporary_download_path
        %td= setting.download_path
        %td= link_to 'Show', setting
        %td= link_to 'Edit', edit_setting_path(setting)
        %td= button_to "Destroy", setting, form: {data: {turbo_confirm: "are you sure?" } }, method: :delete
%br/
= link_to 'New Setting', new_setting_path
